"""ブランド表示設定。

アプリ名・サブタイトル・メニュー名・アイコンをまとめる場所。
画面側のコードにこれらの文字列を直接書かない。

このファイルの変更だけで完結するもの:
  - 既存メニューの名称変更・アイコン変更・表示順変更（MENU_ICONSの編集のみ）
  - アプリ名・サブタイトルの変更（APP_ICON/APP_NAME/APP_SUBTITLEの編集のみ）

このファイルの変更だけでは完結しないもの:
  - 新しい業務画面の追加: MENU_ICONSへの追加に加えて、app.py側のルーティング
    （`elif page=='新画面名': ...render()`）の追加と、対応するrender関数の実装が
    別途必要（app.pyはページ名によるif分岐でrender関数を呼んでおり、
    MENU_ICONSだけでは呼び出し先を持たないため）。
  - 将来、画面追加まで含めて設定変更だけで完結させたい場合は、画面名・アイコン・
    render関数を1つのルート定義（例: リストオブディクトやdataclass）にまとめ、
    app.py側はそのリストをループしてルーティングする構成へ発展させる余地がある
    （今回は大規模リファクタリングを避けるため見送り）。
"""

APP_ICON = "🎹"
APP_NAME = "Piano Studio Manager"
APP_SUBTITLE = "Management System"

# 表示順・アイコンは辞書の定義順がそのままメニュー順になる。
# 既存メニューの名称変更・アイコン変更・並び替えはここだけを編集すればよい。
# 新しいメニュー（業務画面）を増やす場合は、ここへの追加に加えて
# app.pyのルーティング（elif page==...)と対応するrender関数の追加が必要。
MENU_ICONS = {
    "今日の受付": "📅",
    "生徒管理": "👨‍🎓",
    "月次請求": "💴",
    "発表会費請求": "🎼",
    "売上管理": "📊",
    "未入金一覧": "⚠️",
    "例外処理": "🔧",
    "データ・バックアップ": "💾",
}
MENU_PAGES = list(MENU_ICONS)

# 操作担当者の選択肢。将来教室ごとに役職名を変える場合はここを編集する。
OPERATOR_ROLES = ["先生", "受付担当", "代講講師"]
